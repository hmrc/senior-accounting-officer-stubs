info:
  name: signup config - reset (DPS 201)
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/test-only/signup/config"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}
  body:
    type: json
    data: |-
      {
        "safeId": "1234567890"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
