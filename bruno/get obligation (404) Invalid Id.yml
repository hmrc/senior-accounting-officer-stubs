info:
  name: get obligation (404) Invalid Id
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/obligation/456"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
