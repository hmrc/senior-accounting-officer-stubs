info:
  name: Stub Post Etmp Subscription (400)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/test-only/signup/config"
  body:
    type: json
    data: |-
      {
        "utr": "{{utr}}",
        "postEtmpSubscription": {
          "status": 400,
          "defaultBodyOverride" : {{payload}}
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const payload = JSON.stringify(`{
          "origin": "HoD",
          "response": {
            "error": {
              "code": "400",
              "message": "string",
              "logID": "F51FC4C29B667B3BC5D2331E231786F7"
            }
          }
        }`);

        bru.setVar("payload", payload);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
