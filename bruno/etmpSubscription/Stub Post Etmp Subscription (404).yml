info:
  name: Stub Post Etmp Subscription (404)
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/test-only/signup/config"
  body:
    type: json
    data: |-
      {
        "utr": "{{utr}}",
        "postEtmpSubscription": {
          "status": 404
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
