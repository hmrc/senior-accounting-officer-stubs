info:
  name: Stub Post Etmp Subscription (422) 002 already subscribed
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/test-only/signup/config"
  body:
    type: json
    data: |-
      {
        "utr": "{{utr}}",
        "postEtmpSubscription": {
          "status": 422,
          "defaultBodyOverride" : {{payload}}
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const payload = JSON.stringify(`{
          "errors": {
            "processingDate": "2026-01-31T10:26:17Z",
            "code": "002",
            "text": "Business Partner already subscribed for DSAO",
            "dsaoIdNumber": "XB0000493000308"
          }
        }`);

        bru.setVar("payload", payload);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
