info:
  name: Get Subscription (401) invalid Authorization
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/iv_subscriptions/{{subscriptionId}}"
  headers:
    - name: Authorization
      value: Basic not a valid token
    - name: correlationId
      value: "{{$randomUUID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
