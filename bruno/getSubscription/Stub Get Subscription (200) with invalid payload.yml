info:
  name: Stub Get Subscription (200) with invalid payload
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/test-only/post-signup/config"
  body:
    type: json
    data: |-
      {
        "subscriptionId": "{{subscriptionID}}",
        "getSubscriptionAndPostRetrieveCustomerId": {
          "api": "GetSubscription",
          "status": 200,
          "defaultBodyOverride" : "{}"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
