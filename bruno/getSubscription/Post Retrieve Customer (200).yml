info:
  name: "Post Retrieve Customer (200) "
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/business-tax/corporate-tax/iv_subscriptions/{{subscriptionId}}"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}
    - name: correlationId
      value: "{{$randomUUID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
