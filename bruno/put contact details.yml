info:
  name: put contact details
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/contact-details/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
