info:
  name: put subscription
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/contact-details/123"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
