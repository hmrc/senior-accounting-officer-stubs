info:
  name: get contact details unhappy path
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/contact-details"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
