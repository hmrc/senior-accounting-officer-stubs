info:
  name: get obligation (200)
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/obligation/123"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
