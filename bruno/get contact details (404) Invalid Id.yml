info:
  name: get contact details (404) Invalid Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/contact-details/456"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
