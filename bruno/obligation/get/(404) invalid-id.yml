info:
  name: (404) invalid-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/obligation/456"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
