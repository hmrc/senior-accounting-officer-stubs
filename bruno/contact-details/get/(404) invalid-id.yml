info:
  name: (404) invalid-id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/contact-details/456"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
