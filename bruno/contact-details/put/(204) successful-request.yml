info:
  name: (204) successful-request
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/contact-details/123"
  headers:
    - name: Authorization
      value: Basic {{base64Password}}
  body:
    type: json
    data: |-
      [
        {
          "name": "Jane Doe",
          "email": "jane.doe@acme.com"
        }, 
          {
          "name": "John Smith",
          "email": "john.smith@acme.com"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
